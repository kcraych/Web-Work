@model IEnumerable<Moving_Mountains.Models.MountainRange>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.rangeName)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.rangeTerritory)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.rangeSqMiles)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.rangeName)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.rangeTerritory)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.rangeSqMiles)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.rangeId }) |
            @Html.ActionLink("Details", "Details", new { id=item.rangeId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.rangeId })
        </td>
    </tr>
}

</table>
