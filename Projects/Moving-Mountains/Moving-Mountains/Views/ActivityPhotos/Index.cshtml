@model IEnumerable<Moving_Mountains.Models.ActivityPhoto>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.completeId)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.myActivityPhoto)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.completeId)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.myActivityPhoto)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.photoId }) |
            @Html.ActionLink("Details", "Details", new { id=item.photoId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.photoId })
        </td>
    </tr>
}

</table>
